# event-api
Event Registration System API built with Node.js supporting event creation, user registration, cancellation, and event listing with seat limits, validation rules, and duplicate prevention. Uses layered architecture and JSON-based storage with proper error handling and data consistency.
